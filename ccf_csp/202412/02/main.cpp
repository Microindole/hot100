#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n + 1, 0), b(n + 1, 0), w(n + 1, 0);

    for (int i = 1; i < n + 1; i++) {
        cout << w[i] << " ";
    }
    return 0;
}
/*
i -> i+1:



*/